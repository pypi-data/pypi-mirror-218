[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pytest-runner",
    "setuptools_scm",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = ["py38", "py39", "py310"]
