[build-system]
requires = ["hatchling", "hera"]
build-backend = "hatchling.build"

[project]
name = "pipekit-sdk"
version = "0.0.1"
authors = [
  { name="Aleksa Trajkovic", email="aleksa@pipekit.io" },
]
description = "Pipekit Python SDK"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

dependencies = [
  "hera",
]
