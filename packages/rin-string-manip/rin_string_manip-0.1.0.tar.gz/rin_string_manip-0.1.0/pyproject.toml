[tool.poetry]
name = "rin-string-manip"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "rin_string_manip"}]

[tool.poetry.dependencies]
python = "^3.10"
typer = "^0.9.0"
pyyaml = "^6.0"

[tool.poetry.scripts]
rin_string_manip = "rin_string_manip.main:app"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
