[build-system]
requires = ["setuptools>=61.0", "pytest"]
build-backend = "setuptools.build_meta"

[project]
name = "coverage_checker_zcool"
version = "0.0.1"
authors = [
  { name="Omar Diaz", email="zcool2005@gmail.com" },
]
description = "Package to check the code coverage"
readme = "README.md"
requires-python = ">=3.7"
