[tool.poetry]
name = "primapy-pandora"
version = "0.1.0"
description = ""
authors = ["Devops <devops@prima.it>"]
readme = "README.md"
packages = [{include = "primapy_pandora"}]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
