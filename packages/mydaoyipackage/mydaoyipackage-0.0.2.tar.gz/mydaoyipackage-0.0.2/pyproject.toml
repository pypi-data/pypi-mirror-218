[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mydaoyipackage"
version = "0.0.2"
dependencies = [
    "requests",
    "pytest; python_version>'3.1'",
]