from setuptools import setup

setup(name='bino_gauss_package',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['bino_gauss_package'],
      zip_safe=False)
