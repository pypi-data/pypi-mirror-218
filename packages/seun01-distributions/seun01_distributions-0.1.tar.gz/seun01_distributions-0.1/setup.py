from setuptools import setup

setup(name='seun01_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['seun01_distributions'],
      author = 'Aboderin Samuel',
      zip_safe=False)
