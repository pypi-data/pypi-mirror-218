[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dashboard-listener-raku"
version = "0.0.1"
authors = [
  { name="Guilherme Santana", email="ts-guilherme.santana@rakuten.com" },
]
description = "Web Listener code for Robot Framework"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]